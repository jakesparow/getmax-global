# Moved — see `getmax-platform/docs/CLI_HANDOFF_LANDING_MARKETING.md`

This repo (`getmax-global`) is a **stale Vite/React mirror** that deploys only to
`getmax-global.vercel.app`. It does **NOT** drive `getmaxglobal.com`.

The live marketing site at `getmaxglobal.com` is served by **`getmax-platform`** (Next.js 16),
which middleware-classifies root vs `app.` per `feedback/domain_strategy`. The canonical
landing/marketing CLI brief lives at:

> `getmax-platform/docs/CLI_HANDOFF_LANDING_MARKETING.md`

If you're a CLI session that landed here looking for landing-page work, switch repos.

— Pointer left 2026-05-16. Do not duplicate handoff docs.
